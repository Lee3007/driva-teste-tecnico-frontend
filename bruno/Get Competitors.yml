info:
  name: Get Competitors
  type: http
  seq: 5

http:
  method: GET
  url: "{{BASE_URL}}/api/competitors"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
