info:
  name: Get Demand
  type: http
  seq: 6

http:
  method: GET
  url: "{{BASE_URL}}/api/demand"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
