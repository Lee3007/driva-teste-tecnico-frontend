info:
  name: Get Stores
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/api/stores"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
