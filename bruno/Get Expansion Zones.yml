info:
  name: Get Expansion Zones
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/api/expansion-zones"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
