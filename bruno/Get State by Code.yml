info:
  name: Get State by Code
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/api/states/SP"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
